#include <stdio.h>
#include <err.h>

#include "seg.h"
#include "rule.h"

extern struct seg allseg[];

int
main()
{
	int i, j;
	struct seg *s, *t;

	/* If given a language (or all), check the sanity of rules */

	/* apply every rule we know to every relevant segment
	 * and check that the result is what it should be. */

	return 0;
}
